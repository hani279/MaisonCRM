services:
  - type: web
    name: maisons-crm-demo
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DEMO_SEED
        value: "true"
      - key: NODE_ENV
        value: production
